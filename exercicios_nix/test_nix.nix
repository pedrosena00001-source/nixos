#let vai declarar a variavel 
let 
        nome = "Pedro";
#vai ser como o print do codigo ent ele vai soltar no sistema o que foi declarado e funções
in
        "Seu nome tem ${toString (builtins.stringLength nome)} letras."
